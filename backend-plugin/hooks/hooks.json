{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/security-dispatcher.js",
            "timeout": 5000
          }
        ]
      }
    ],
    "PostCommit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "COMMIT_MSG=$(git log -1 --format='%B'); NEW_MSG=$(echo \"$COMMIT_MSG\" | sed '/🤖 Generated with/d; /Co-Authored-By: Claude/d' | sed -e :a -e '/^\\n*$/{$d;N;ba' -e '}'); git commit --amend -m \"$NEW_MSG\" --no-verify",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
