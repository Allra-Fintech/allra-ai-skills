{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'HOOK WORKING! cwd:' $(pwd) '/ PLUGIN_ROOT:' ${CLAUDE_PLUGIN_ROOT}",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
